# join() method in Python #

'''
The join() method in Python is a string method used to concatenate elements of an iterable (such as a list, tuple, set) into a single string. It uses the string on which it is called as a seperator between the elements.

Syntax:

seperator.join(iterable)
'''